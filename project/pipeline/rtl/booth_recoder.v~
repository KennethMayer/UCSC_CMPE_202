/*
 * Kenneth Mayer (1401529)
 * krmayer@ucsc.edu
 * booth_recoder.v
 * This module performs Booth Recoding on the 3-bit input and returns the 
 * encoded value.
 */

module booth_recoder(
    input  [2:0] value_i,

    output [2:0] encoded_value_o,
);

   always@(*) begin
      
   end

endmodule // booth_recoder

   
